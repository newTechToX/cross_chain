package matcher

/*func (m *Matcher) collectUnfetchedData(svc *svc.ServiceContext, project string, from, to uint64) {
	var collector model.Colletcor
	switch project {
	case "anyswap":
		collector = anyswap.NewAnyswapCollector(svc)
	case "across":
		collector = across.NewAcrossCollector()
	case "synapse":
		collector = synapse.NewSynapseCollector(svc)
	default:
		collector = nil
	}

}*/
